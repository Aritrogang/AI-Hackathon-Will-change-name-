# === Katabatic Frontend — Environment Variables ===
# Copy this to .env

# Backend API URL (local dev default)
VITE_API_URL=http://localhost:8000

# Set to true to use mock data without a running backend
VITE_DEMO_MODE=false
